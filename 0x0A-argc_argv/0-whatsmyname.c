#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: count of argument passed
 * @argv: pointer of array of string passed to
 *
 * Return: Alway 0.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
